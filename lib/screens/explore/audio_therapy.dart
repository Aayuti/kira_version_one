import 'package:flutter/material.dart';

class AudioTherapy extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Audio therapy')),
      body: Center(child: Text('Coming Soon!')),
    );
  }
}
